# 平台 A

平台 A 用于长文、深度分析、结构化论证和方法论内容。

## 目录

- `drafts/`：未确认草稿。
- `final/`：用户确认后的最终稿。
- `assets/`：素材。
- `retros/`：复盘。
- `samples/`：样本。
- `scripts/`：脚本或内容工作流输入。
- `predictions/`：发布前预测。

## 使用规则

未确认内容只能放入 `drafts/`。

进入 `final/` 前必须经过用户确认。

