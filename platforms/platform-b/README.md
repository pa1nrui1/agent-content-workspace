# 平台 B

平台 B 用于图文、图片卡片、过程展示、工具展示和收藏型内容。

## 目录

- `drafts/`：未确认草稿。
- `final/`：用户确认后的最终稿。
- `assets/`：素材。
- `retros/`：复盘。
- `samples/`：样本。
- `scripts/`：脚本或内容工作流输入。
- `predictions/`：发布前预测。

## 使用规则

图文内容发布前必须检查标题、首图、前两行正文和多图递进。

进入 `final/` 前必须经过用户确认。

