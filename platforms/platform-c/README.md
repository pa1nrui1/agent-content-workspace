# 平台 C

平台 C 用于短视频、口播、节奏推进和连续系列内容。

## 目录

- `drafts/`：未确认草稿。
- `final/`：用户确认后的最终稿。
- `assets/`：素材。
- `retros/`：复盘。
- `samples/`：样本。
- `scripts/`：脚本或内容工作流输入。
- `predictions/`：发布前预测。

## 使用规则

视频内容发布前必须单独检查前三秒、前十秒、节奏推进和结尾讨论口。

进入 `final/` 前必须经过用户确认。

